name: Simple Start Up

on:
  push:
    branches: [main]
#  schedule:
#   - cron: '* * * * *'
    
jobs:
  my-job:
    name: My First Job
    runs-on: ubuntu-latest
    steps:
    - name: Print a greeting
      run:  |
        echo Hello there!!
